常见的时间复杂度（按效率排序）
O(1) < O(logn) < O(n) < O(nlogn) < O(n^2) < O(n^2logn) < O(n^3)
复杂问题的时间复杂度
O(n!) O(2^n) O(n^n)

快速判断算法复杂度（适用于绝大部分简单情况）：
确定问题规模n
循环减半过程logn
k层关于n的循环 n^k

空间复杂度 用来评估算法内存占用大小的式子
 1 算法使用了几个变量 O(1)
 2 算法使用了长度n的一维列表 O(n)
 3 算法使用了m行n列的二维列表 O(mn)
一般是时间换空间